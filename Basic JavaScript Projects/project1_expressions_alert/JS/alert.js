var sent1 = "this is the beginning of the string" // this sets sent1 to the string in quotes 
var sent2 = " and this is the end of the string" // this does the same for the sent2 variable to what is in quotes

alert(sent1 + sent2); // this concatinates them together and sends the alert which has an expression. 
