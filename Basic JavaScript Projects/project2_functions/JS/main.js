function myFunction() { //making my function
    var sentence = "hi im"; // setting a variable equal to the string "hi im"
    sentence += " scott"; // adding onto the variable " scott" with +=

    document.getElementById("new_assignment").innerHTML = sentence; // using document to get to the html. Then using the IDby to get where
    // thing i want to modify is in my html. Then I set it to be the value of sentence. 

}
